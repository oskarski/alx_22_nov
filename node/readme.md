```
npm init <- stworz nowy projekt
npm install nazwa-biblioteki <- zainstaluj koknretna biblioteke
npm install <- zainstaluj biblioteki zdefiniowane w projekcie
npm install --save-dev nazwa-biblioteki <- zainstaluj konkretna biblioteke jako zaleznosc deweloperska 
npm uninstall nazwa-biblioteki <- odinstaluj konkretna biblioteke

package.json <- ma informacje o skryptach i zaleznosciach
package-lock.json <- ma szczegolowe informacje o wersjach zaleznosci|
node_modules <- katalog z bibliotekami ( pamietamy o dodaniu do .gitignore )
```